# 🤖 Genaja Agent Executive Protocol (Platinum v0.7.1)

If you are an AI Agent assisting in this repository, you **must** adhere to these engineering constraints to maintain project integrity and "One-Click Maintainability."

## 1. Zero Hardcoding Rule
- **NEVER** write a static version string (e.g., "0.7.1") in any file other than `src/version.py`.
- **NEVER** manually update versions in `README.md`, `CHANGELOG.md`, `Cargo.toml`, or `module_versions.json`.
- **ACTION**: Use `python scripts/automate.py --fix` to propagate version changes across the ecosystem.

## 2. Mandatory Module Declaration
- Every functional module in `src/` **must** include the version hook at the end of the file.
- **TEMPLATE**:
  ```python
  from version_hook import declare as _vdeclare
  from version import __version__
  _vdeclare(__name__, __version__, "Descriptive changelog entry")
  ```

## 3. The "Zombie" Purge
- Always audit for unused files (Zombies) before finalizing a task.
- If a module is removed, its corresponding tests **must** be removed or archived.
- Maintain the `src/core/adapters` and `src/core/services` directory topology; avoid creating root-level utility folders like `src/utils` or `src/adapters`.

## 4. Boot & Logic Integrity
- Always run `python scripts/automate.py --quick` after structural changes to ensure the Flet initialization and core engines are still operational.
- Use `LoggerService` for all runtime messaging. Do not use print statements in core engines.

---
**Status**: GENAJA PLATINUM CERTIFIED. DO NOT BREAK THE CHAIN.
